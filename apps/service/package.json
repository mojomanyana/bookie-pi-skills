{
  "name": "@bookie/service",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -b",
    "build:force": "tsc -b --force",
    "test": "npm run build && node --test test/*.test.mjs",
    "typecheck": "tsc -b --pretty false"
  },
  "dependencies": {
    "ajv": "^8.20.0",
    "ajv-formats": "^3.0.1",
    "fastify": "^5.12.4"
  }
}
